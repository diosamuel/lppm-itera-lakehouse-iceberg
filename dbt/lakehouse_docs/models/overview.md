{% docs __overview__ %}

# LPPM ITERA Lakehouse Documentation

This dbt project is used as a manual documentation layer for the existing
Trino and Iceberg lakehouse.

Use `models/sources.yml` to maintain descriptions for schemas, tables, and
columns. The project does not need SQL models or `dbt run` to publish docs.

{% enddocs %}
