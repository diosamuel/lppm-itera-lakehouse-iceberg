# Host-side PySpark Spark Connect client requirements.
# pyspark version MUST exactly match the server Spark version (connect protocol is versioned).
pyspark==3.5.6
grpcio==1.73.0
grpcio-status==1.73.0
protobuf>=4.25.0
